## Packages
framer-motion | Essential for ritualistic animations and reveals
lucide-react | Icons for UI elements
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
- Fingerprinting logic will be implemented client-side using native Web Crypto API (SHA-256).
- No external heavy libraries for fingerprinting to keep it lightweight as requested.
- Theme relies on specific Google Fonts (Cinzel for headings, Cormorant Garamond for body).
